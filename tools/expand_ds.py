#!/usr/bin/env python3
"""扩展专题五(Deepseek篇)12 篇内容：加更多子节、更深机制、更具体数字、加厚口述版。
替换 _raw/docs 对应 json 的 content/word_count，再跑 build_data.py 即可。"""
import json, os, re, time

BASE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
DOCS = os.path.join(BASE, "_raw", "docs")


def wc(html):
    return len(re.sub(r"<[^>]+>", " ", html).strip())


def wrap(html):
    return ('<!doctype lake><meta name="doc-version" content="1" /><meta name="viewport" content="fixed" />'
            '<meta name="typography" content="classic" /><meta name="paragraphSpacing" content="relax" />' + html)


DEEPSEEK = {
"ds5q01arch": ("1.DeepSeek整体架构总览",
 "<h3>答题思路</h3><p>面试官让你讲 DeepSeek-V3 的整体架构，其实是在考你对「当前开源大模型工程化前沿」的整体判断力。回答的层次应该是：先用一句话点出它的定位（671B 总参、37B 激活的 MoE），再讲它围绕「省算力、提密度、保稳定」这条主线做的四件事——MoE 降激活、MLA 降 KV、MTP 提密度、FP8+无损均衡保训练稳，最后落到它为什么能又大又快又便宜。不要平铺直叙报参数，要讲每项解决了什么瓶颈、彼此怎么咬合。</p>"
 "<h2>定位：总参 671B、激活 37B 的细粒度 MoE</h2>"
 "<p>DeepSeek-V3 是一个总参数 671B、单 token 激活约 37B 的稀疏 MoE 模型，整体仍是 Transformer 块堆叠，但每个块的注意力换成了 MLA，FFN 换成了 DeepSeekMoE。把总参与激活拆开看是理解它的第一把钥匙：总参大意味着容量大、知识多，激活小意味着每个 token 实际算的少，所以它能在「质量逼近甚至超过更大稠密模型」的同时把推理算力压到同体量稠密模型的一小部分。这正是开源阵营能和闭源大模型掰手腕的底牌。</p>"
 "<p>MoE 部分的结构是：每个 MoE 层由一个共享专家（始终激活）加 256 个细粒度路由专家（每 token 选 8 个激活）组成。共享专家兜通用知识，路由专家管专的长尾，这是 DeepSeekMoE 的知识分工哲学，下一题细讲。这里要记住的数字是：671B 总参里，激活约 37B——其中包含共享专家和被命中的 8 个路由专家。</p>"
 "<h2>三块省钱的硬骨头</h2>"
 "<p>第一块是 MLA（多头潜在注意力）。标准多头注意力的 KV 缓存随层数和头数线性增长，长上下文下显存吃紧、并发上不去。MLA 把 KV 压成一个低秩潜在向量，推理时再上投影还原，KV 缓存占用下降约 75%，于是同样显存能撑更长上下文、更大 batch。第二块是 MoE 本身：激活 37B 而非全量 671B，单位 token 计算量大幅下降，这是它「又大又快又便宜」的根。第三块是 MTP（多 token 预测），训练时让模型同时预测后续多个 token，推理时可做投机解码加速，解码阶段吞吐提升明显。</p>"
 "<h2>训练侧的两个稳定器</h2>"
 "<p>传统 MoE 靠辅助损失做负载均衡，容易和主任务互相拉扯、拖累收敛。DeepSeek-V3 用「辅助无损负载均衡」——靠每个专家的偏置项动态调节路由概率，让负载自然摊平，不引入额外损失项，训练更稳。再叠加 FP8 混合精度训练，在保证数值稳定的前提下把矩阵乘和跨卡通信都压到 8 bit，训练成本进一步下降。这两项加上前面三块，合起来就是 DeepSeek-V3 训练成本远低于同体量模型的原因——据公开信息，V3 全量训练算力约 553 万 H800 小时，相比同规模模型是数量级的节省。</p>"
 "<h2>协同关系：不是堆料，是一条线</h2>"
 "<p>容易被追问的点：这些创新是不是各自孤立堆上去的？答案是它们围绕「省算力、提密度、保稳定」协同设计。MoE 省激活算力，但专家一多负载均衡就难，于是配无损均衡来保稳；MLA 省 KV 显存，让长上下文和并发撑得住，间接放大了 MoE 省算力的收益（同样硬件跑更多请求）；MTP 提训练信号密度又提推理吞吐；FP8 把训练侧的算和通信一起省。四者咬合，才有了「同体量里成本最低、效果还能压着很多更贵模型」的结果。</p>"
 "<h3>面试口述版</h3><p>DeepSeek-V3 我会这样概括：它是一个 671B 总参、37B 激活的 MoE 模型，整体还是 Transformer，但三处关键改造。注意力换成 MLA，把 KV 缓存压到原来的四分之一左右，所以长文本和并发都能撑住。FFN 换成细粒度 MoE 加共享专家——一个共享专家始终激活兜通用知识，256 个路由专家每 token 选 8 个，激活参数少、算得快，专家分得细知识也学得专。再加上多 token 预测，训练信号更密、推理能投机解码。训练侧两个点很值得说：一个是辅助无损负载均衡，靠专家偏置动态调、不靠额外 loss，所以稳；另一个是 FP8 混合精度训练，把矩阵乘和跨卡通信都压到 8 bit，训练成本数量级下降。这几个点不是孤立堆上去的，是围绕省算力、提密度、保稳定这条线协同设计的，这也是它成本低、能放开源、还能压着很多闭源模型打的根本。补充一个数字：V3 全量训练大约 553 万 H800 小时，相比同规模模型省了一个数量级。</p>"),

"ds5q02mla": ("2.MLA多头潜在注意力原理",
 "<h3>答题思路</h3><p>这道题考的是对注意力机制显存瓶颈的深度理解。答 MLA 不能只说「它压缩了 KV」，要讲清楚压缩在哪、为什么能还原、和 GQA/MQA 比强在哪、代价是什么。核心是「低秩联合压缩」——把多头的 K、V 不分头存，而是先投影到一个共享的低维潜在向量，缓存只存这个潜在量，推理时再做上投影恢复出各头的 KV。</p>"
 "<h2>瓶颈在哪：KV 缓存的线性增长</h2>"
 "<p>标准多头注意力推理时，每个 token 在每一层每一头都要存 K、V 两份向量。KV 缓存量 = 2 × 层数 × 头数 × 头维 × 序列长度 × batch。层数深、头数多、上下文长，KV 缓存就成了显存大头，也限制了能并发的请求数。长上下文场景下，KV 缓存经常比模型权重还大，是推理服务的头号瓶颈。</p>"
 "<h2>GQA/MQA：共享式折损</h2>"
 "<p>GQA 把多头的 K/V 归组共享（比如每 4 头一组），MQA 更激进让所有头共享一组。它们都是「以牺牲表达力换显存」——组越大越省、但越像单头注意力，质量有回退。GQA 是当下主流稠密模型常用的折中，但在超大模型和超长上下文下，它的节省还不够。</p>"
 "<h2>MLA 的做法：压缩到潜在空间再还原</h2>"
 "<p>MLA 不走「共享」的路，而是走「低秩压缩」。对每一层的注意力，先通过下投影矩阵把 K、V 联合压成一个维度远低于「头数 × 头维」的潜在向量 c（KV 联合压缩，共享一个潜在量）；缓存里只存这个 c。计算注意力时，再用上投影矩阵把 c 恢复成各头的 K、V。因为 c 的维度设得很低，KV 缓存占用降到大约四分之一，同时由于是「可逆上投影」而非「共享折损」，表达力基本不掉。</p>"
 "<p>具体到 V3 的数字：MLA 把每层每头的 KV 压成一个维度约 512 的联合潜在向量（而未压缩的 KV 维度是头数×头维，量级大得多），缓存降到约 25%。配合 61 层、128 头的规模，长上下文下省下的显存非常可观，这是 V3 能撑超长上下文、跑高并发的关键。</p>"
 "<h2>代价与工程取舍</h2>"
 "<p>一个常被追问的点：上投影矩阵也很大，存它不也费显存吗？答案是上投影矩阵是模型权重的一部分，是常驻的、不随 token 增长的，和 KV 缓存这种「随上下文线性增长」的开销不在一个量级。MLA 省的是随序列长度膨胀的那部分，这正是长上下文和并发场景的真正痛点。代价是上投影带来额外计算，但这部分可融合进注意力算子优化，整体仍是净收益——尤其在长上下文下，省显存带来的吞吐提升远大于多算的那点开销。</p>"
 "<h2>和 GQA 的本质区别</h2>"
 "<p>GQA 是「让多头退化成少头」来省缓存，丢的是头间表达差异；MLA 是「把缓存压到低秩再还原」，保住的是头间差异、省的是存储。所以 MLA 在同等缓存下质量更高，这也是 DeepSeek 选择它而非继续推 GQA 的原因。代价是工程复杂度——上投影、下投影、注意力计算要定制算子，不像 GQA 直接复用标准注意力实现。</p>"
 "<h3>面试口述版</h3><p>MLA 我会这么讲：传统注意力推理慢，主要慢在 KV 缓存随上下文线性增长，长上下文下缓存经常比模型权重还大。GQA、MQA 是让多头共享 K/V 来省，本质是让多头退化成少头，丢的是头间表达差异。MLA 换了个思路——做低秩压缩。每个 token 在每一层先被投影成一个很短的联合潜在向量，缓存只存这个向量；要用的时候再上投影还原成各头的 K 和 V。因为潜在维度设得低，V3 里 KV 缓存大概压到四分之一；又因为它是可还原的投影而不是共享折损，头间差异保住、质量基本不掉。上投影矩阵虽然不小，但它是权重、不随长度涨，和 KV 缓存的线性增长不是一个量级。所以长上下文和高并发场景下 MLA 比共享式方案更划算。和 GQA 的本质区别：GQA 是让多头退化省缓存丢表达，MLA 是压到低秩再还原省存储保表达，同等缓存下质量更高，代价是算子要定制、工程更复杂。这也是 DeepSeek 选 MLA 不继续推 GQA 的原因。</p>"),

"ds5q03moe": ("3.DeepSeekMoE细粒度专家与共享专家",
 "<h3>答题思路</h3><p>这道题考的是 MoE 设计的「知识分配」哲学。答好它要讲两件事：为什么专家要切细，以及为什么额外留一个共享专家。核心矛盾是——专家越多越专，但通用知识（语法、常识、基础推理）如果分散到各专家里，路由一抖动就没人兜底。DeepSeekMoE 的解法是「细粒度路由专家 + 强制共享专家」。</p>"
 "<h2>传统 MoE 的问题：大专家容易重叠和塌缩</h2>"
 "<p>传统 MoE（如 Switch Transformer、GShard 风格）倾向用少量大专家，比如 8 个，每个专家容量大、学的范围宽。这有两个毛病：一是知识容易重叠——几个专家学到相似的东西，浪费容量；二是路由塌缩——token 倾向往某几个专家跑，导致负载不均、训练不稳，需要辅助损失硬拽。</p>"
 "<h2>细粒度：把一个大专家拆成多个小专家</h2>"
 "<p>DeepSeekMoE 把专家切细：更多个、更小的路由专家（V3 是 256 个路由专家），每个 token 同时激活其中若干个（V3 是 8 个）。专家小而多，单个专家学的更专、知识重叠更少，组合起来能表达更精细的能力。配合细粒度路由，每个 token 由一组小专家组合服务，相当于「专家集成」而非「专家二选一」。打个比方：与其让 8 个全科医生轮班，不如让 256 个专科医生按病情组队，组合更灵活、覆盖更细。</p>"
 "<h2>共享专家：给通用知识一个固定落脚点</h2>"
 "<p>只靠细粒度路由专家有个隐患：通用知识——语法结构、基础逻辑、常识——几乎每个 token 都要用，如果也走路由，要么每个专家都重复学一份、浪费容量，要么路由不稳时通用能力就抖。DeepSeekMoE 的做法是设一个或几个「共享专家」，每个 token 无条件都过它，让它专门承载这些共性知识。路由专家只负责更专的长尾知识。这样分工后，通用能力稳定有兜底，专家容量不浪费在重复上，整体质量更稳。</p>"
 "<h2>数字与协同</h2>"
 "<p>V3 的具体配置：256 个路由专家 + 1 个共享专家，每 token 激活 8 个路由专家加共享专家，激活约 37B。共享专家始终在岗，保证每个 token 的通用计算不缺位；路由专家按 token 动态组合，提供专的能力。这种「共享兜底 + 细粒度专精」的双层结构，是 DeepSeekMoE 区别于传统 MoE 的核心。它和后面的无损负载均衡是配套的——专家一细，均衡更难，正好用无损均衡来稳住。</p>"
 "<h2>为什么细粒度反而更省</h2>"
 "<p>一个反直觉的点：专家变多了，总参数不是更大吗？是的，总参更大（671B），但激活量（37B）由「每 token 选几个」决定，和专家总数无关。所以可以把专家做小做多做专，总参堆上去换容量，激活控制在低水平换算力，这是 MoE 的精髓。DeepSeekMoE 把这个精髓推到「细粒度 + 共享」的极致，兼顾了容量、专精度和算力。</p>"
 "<h3>面试口述版</h3><p>DeepSeekMoE 的设计我会拆两点讲。第一，专家切细：传统 MoE 用几个大专家，容易知识重叠、路由扎堆；它把专家拆成很多小专家——V3 是 256 个路由专家，每 token 选 8 个激活，单个专家学得更专、组合起来表达更精细，相当于专家集成而非二选一。第二，也是最关键的一招——共享专家：通用知识像语法、常识几乎每个 token 都要用，如果也走路由，要么每个专家重复学、要么路由一抖就没人兜底。所以它专门留一个共享专家，每个 token 无条件都过，把共性知识稳稳兜住，路由专家只管专的长尾。V3 配置是 256 路由专家加 1 共享专家，每 token 激活 8 个路由加共享，激活约 37B。一个反直觉的点：专家变多总参更大，但激活量由每 token 选几个决定、和专家总数无关，所以可以把专家做小做多做专换容量、激活控低换算力，这就是 MoE 精髓，DeepSeekMoE 把它推到细粒度加共享的极致。</p>"),

"ds5q04bal": ("4.无辅助损失负载均衡",
 "<h3>答题思路</h3><p>这道题考的是 MoE 训练稳定性里很具体的一个点。答的时候先讲传统辅助损失为什么有问题，再讲 DeepSeek 的「无损」方案到底损了什么、无损在哪，最后讲它的动态偏置怎么调。核心是：把负载均衡从「加一个损失项去逼」改成「调每个专家的偏置去引」。</p>"
 "<h2>老办法：辅助损失及其代价</h2>"
 "<p>传统 MoE 为了防止路由塌缩（所有 token 都往少数专家跑），会加一个辅助损失项惩罚负载不均。比如让每个专家被选中的概率尽量均匀，偏离就加 loss。问题是这个辅助损失和主任务目标是两个方向：主任务要预测对，辅助损失要负载平，二者会互相拉扯。尤其到训练后期，模型已经学得不错了，辅助损失还在硬拽负载分布，反而拖累模型质量和收敛。这是个「用损失换均衡」的妥协，调起来还要给辅助损失配权重，超参敏感。</p>"
 "<h2>DeepSeek 的做法：偏置项动态调节</h2>"
 "<p>DeepSeek-V3 用的是「辅助无损负载均衡」（auxiliary-loss-free）。思路是不再加额外损失项，而是给每个专家配一个偏置项 b，加到该专家的路由 logit 上。偏置项的大小根据该专家最近一段时间的实际负载动态调整——负载高就降偏置（让 token 少往这跑）、负载低就升偏置（让 token 多往这跑），靠偏置把 token 流量自然引向欠载专家。</p>"
 "<p>关键在于：偏置是在路由打分阶段加的，它影响的是「token 往哪走」，但不进入损失函数。所以主任务目标不被拉扯，这就是「无损」的含义——无损于主任务。同时偏置项是会随训练演化的、不是手设的，它的更新规则简单、开销小，但效果上比固定辅助损失更自适应。</p>"
 "<h2>为什么不直接回填，要靠偏置引</h2>"
 "<p>有人会问：负载不均，直接把溢出的 token 强制塞给空闲专家不就行了？这种「硬均衡」会破坏路由的语义——路由分数本来表示专家和 token 的匹配度，硬塞会让 token 去到不匹配的专家，质量反而掉。偏置项是「软引导」：它调的是路由分数的偏移，让空闲专家在打分上稍微占优，token 自然倾向它，但最终路由还是基于匹配度+偏置综合判，保留了路由的语义合理性。这比硬均衡温柔得多。</p>"
 "<h2>配合互补损失防极端</h2>"
 "<p>DeepSeek 还配了一个很轻的「互补损失」（不是传统负载辅助损失），目的是防单个 token 把所有激活都堆到同一组专家、鼓励一个 token 选的多个专家之间有差异。它权重很小、作用是防退化，和主负载均衡的无损偏置是两件事。回答时可以提一句：无损均衡是主，互补损失是辅助防退化，不要混为一谈。</p>"
 "<h3>面试口述版</h3><p>传统 MoE 怕路由扎堆，就加一个辅助损失去惩罚负载不均。问题是这个辅助损失和主任务方向不一致，会互相拉扯，后期反而拖质量，还超参敏感。DeepSeek 换了个思路，叫辅助无损负载均衡：不加损失项，而是给每个专家加一个偏置，加到路由分数上；偏置根据这个专家最近的真实负载动态调——负载高就把偏置调低、负载低就调高，把 token 流量自然往欠载专家引。因为它是在打分阶段调偏置、不进 loss，主任务不受干扰，所以叫无损。为什么不用硬塞？硬塞会破坏路由语义，把 token 强塞给不匹配的专家质量会掉；偏置是软引导，让空闲专家在打分上微优，token 自然倾向但路由仍基于匹配度加偏置综合判，保留了语义合理性。另外还配了个很轻的互补损失防单个 token 把激活堆到同一组专家，权重很小只防退化。无损均衡是主、互补损失是辅，别混。</p>"),

"ds5q05r1": ("5.DeepSeek-R1如何用纯强化学习涌现推理",
 "<h3>答题思路</h3><p>这道题是 DeepSeek 最有冲击力的点——R1-Zero 证明了「大规模纯 RL 就能让推理能力自己长出来」。回答要讲清楚三件事：起点是什么（V3 基座）、奖励怎么设计（基于规则而非模型打分）、涌现了什么（自我反思、Aha moment）。重点强调「没有用思维链标注数据做 SFT」，这是它和一众蒸馏 o1 路线的根本区别。</p>"
 "<h2>起点与奖励：纯 RL、规则奖励</h2>"
 "<p>R1-Zero 直接拿 DeepSeek-V3 的基座模型开练，不做监督微调，只用大规模强化学习。强化学习算法用的是 GRPO（群体相对策略优化，下一题详讲）。最关键的是奖励设计：它没有用另一个大模型去给回答打分——那种奖励模型容易 reward hacking（模型学会骗打分器）、也贵、还难维护。R1 用的是基于规则的奖励：答案对不对用程序判——数学题能验算、代码能跑测试、逻辑题有标准答案，再加上格式奖励，强制模型把思考过程放进指定标签里。规则奖励简单、可信、不会被骗，这是纯 RL 能稳定推进的前提。</p>"
 "<h2>涌现了什么：Aha moment 与长思考链</h2>"
 "<p>纯 RL 跑着跑着，模型自己长出了反思、自我验证、中途纠错的行为——训练日志里最出名的是模型突然写出「Wait, let me recompute」这种自我打断的句子，被叫做 Aha moment（顿悟时刻）。这意味着推理链长度自主拉长、策略自主变复杂，不是靠人标思维链教出来的，而是奖励信号引导下自己摸索出来的。这一点震撼业界，因为它打破了「推理能力必须靠人类思维链数据灌输」的固有认知——原来只要奖励信号够干净、RL 规模够大，推理就能涌现。</p>"
 "<p>涌现的具体表现：模型学会在卡壳时回头检查、在不确定时生成多种解法择优、在长推理里维护中间结论。这些行为没有被显式训练，是从「答对有奖励」这个目标里自发长出来的策略。这正是 RL 的威力——你定目标，它自己找方法。</p>"
 "<h2>训练模板：约束格式而非约束内容</h2>"
 "<p>R1-Zero 的训练模板设计很克制：它给一个提示模板，要求模型把推理过程放在指定标签里、答案放在另一标签里，但不对推理过程的内容做任何约束。也就是说，模型自由探索怎么思考，模板只管输出格式。这种「只定格式、不定思路」的设计，是推理策略能自由涌现的条件——如果模板里塞了思维链范例，模型会去模仿范例，反而可能限制它发现更好的策略。</p>"
 "<h2>R1-Zero 的问题与 R1 的收口</h2>"
 "<p>但 R1-Zero 也有毛病：可读性差、语言会混（中英文夹杂）、输出啰嗦且格式不规整。对研究它是震撼的证明，对实际使用它不够好用——用户要的是清晰、稳定、规范的可读输出。所以后续 R1 才在它基础上加少量冷启动数据和多阶段训练来修这些问题、再蒸馏到小模型——这就是 R1 与 R1-Zero 的关系。要记住：能力涌现主要归 R1-Zero 的纯 RL，R1 是工程可用性的收口。</p>"
 "<h3>面试口述版</h3><p>R1-Zero 这条路最震撼的地方是它证明了推理能力可以靠纯 RL 自己涌现出来。起点是 V3 基座，不做 SFT，直接大规模强化学习。最关键的设计在奖励：它不用大模型打分——那种容易 reward hacking 还贵还难维护；它用规则奖励——数学能验算、代码能跑测试判对错，再加格式奖励逼模型把思考放进标签里。规则奖励可信、骗不了，这是纯 RL 能稳跑的前提。训练模板很克制，只约束输出格式、不约束思考内容，让模型自由探索策略。跑着跑着模型自己长出了反思、自我验证、中途纠错，最出名的就是日志里那句「Wait, let me recompute」，叫 Aha moment，推理链自主拉长、策略自主变复杂。这打破了推理必须靠人标思维链灌输的认知——原来奖励信号够干净、RL 规模够大，推理就能涌现。当然 R1-Zero 可读性差、语言混，所以才有后面 R1 加冷启动数据和多阶段训练收口、再蒸馏到小模型。但能力涌现主要归 R1-Zero 的纯 RL，这是 DeepSeek 路线和蒸馏 o1 那一派的根本区别。</p>"),

"ds5q06grpo": ("6.GRPO群体相对策略优化",
 "<h3>答题思路</h3><p>这道题考的是 R1 训练用到的核心 RL 算法。回答要把 GRPO 和 PPO 比着讲，突出它省掉了什么、为什么能省、代价是什么。核心一句话：GRPO 把「优势估计」从「需要一个价值网络」改成「同一题采样多个回答、组内相对比较」，所以不用训 critic，省一半算力，还更稳。</p>"
 "<h2>从 PPO 说起：为什么贵且不稳</h2>"
 "<p>PPO 是 RLHF 的经典算法，它需要维护一个价值网络（critic）来估计每个状态的价值，从而算「优势」（advantage，衡量某个动作比平均好多少）。优势 = 奖励 - 价值网络估计。训 critic 本身就要占一份和策略网络差不多大的算力和显存，在大模型规模下这是很重的开销。更麻烦的是：critic 估得不准，优势就有噪声，训练就不稳；而大模型规模下训准一个 critic 极难。所以 PPO 在 LLM 规模上又贵又难稳。</p>"
 "<h2>GRPO：用组内比较代替价值网络</h2>"
 "<p>GRPO（Group Relative Policy Optimization）的做法：对同一个问题，让策略模型采样一组多个回答（比如 G 个），用这一组回答各自的真实奖励减去组内均值，作为每个回答的优势。某回答在组内高于平均就是正优势、低于就是负优势，策略据此更新。因为优势是从「同一题多个样本的真实奖励」相对算出来的，不需要一个价值网络去估，critic 整个被省掉了——算力、显存都省一半，也不存在 critic 估不准的稳定性问题。</p>"
 "<p>打个比方：PPO 是请一个评委（critic）给每个回答估分，估不准就乱；GRPO 是让一组回答互相比，不用评委，谁高谁低组内见真章。后者省了一个评委，还更准——因为组内比较用的是真实奖励，不是估计值。</p>"
 "<h2>为什么这套在 R1 场景特别契合</h2>"
 "<p>GRPO 的组内相对比较，前提是「奖励本身能精确判」。R1 用规则奖励判推理对错（数学验算、代码测试），正好满足这个前提——奖励可信，组内比较就有意义。如果奖励是模糊的模型打分，组内比较的噪声会被放大，GRPO 的优势就削弱。所以「GRPO + 规则奖励」是 R1 这套能跑得起大规模 RL 的关键组合：又准又省。</p>"
 "<h2>KL 正则与稳定性</h2>"
 "<p>GRPO 还会加一个 KL 散度正则，把策略往参考策略（通常是基座或上一轮）上拉，防止策略跑飞。这是 RL 训练稳定性的常规手段，但 GRPO 因为没有 critic，KL 正则的作用更突出——它替 critic 承担了一部分「防止策略漂移过远」的稳定作用。代价是 KL 权重要调好，太大会限制探索、太小会漂移，这是个需要平衡的超参。</p>"
 "<h2>组内相对的额外好处</h2>"
 "<p>组内相对比较还天然有「自我对齐」的味道——模型不用和外部基准比，和自己这组样本比就能分出好坏，信号很干净。而且同一题采多个样本，等于对同一问题做了密集探索，数据利用更充分。这些是 GRPO 在 R1 推理 RL 上效果好的深层原因。</p>"
 "<h3>面试口述版</h3><p>GRPO 一句话讲就是：它把优势估计从「训一个价值网络」改成「同一题采一组、组内相对比」。PPO 的老办法要维护一个 critic 去估每个状态的价值，critic 和策略网络差不多大，算力显存都翻倍，而且大模型规模下 critic 估不准、训练就不稳。GRPO 不训 critic：同一道题让模型采一组多个回答，每个回答的真实奖励减去这组均值就是它的优势——高于平均正、低于平均负，策略照这个更新。critic 整个省掉，算力省一半，也不存在估不准的问题。打个比方，PPO 是请评委估分容易估歪，GRPO 是一组回答互相比、谁高谁低见真章。这套在 R1 场景特别契合，因为奖励能精确判——规则奖励判推理对错，组内比较才有意义，如果奖励是模糊模型打分，GRPO 优势就削弱。所以 GRPO 加规则奖励是 R1 能跑得起大规模 RL 的关键组合。另外 GRPO 会加 KL 正则防策略跑飞，替 critic 承担了一部分稳定作用；组内相对还有自我对齐的味道，信号干净、数据利用充分。</p>"),

"ds5q07zero": ("7.R1-Zero与R1的区别及冷启动数据",
 "<h3>答题思路</h3><p>这道题考的是从 R1-Zero 到 R1 的工程化收口。答的核心是：R1-Zero 证明了能力，R1 解决了可用性。R1-Zero 能力强但难用（可读性差、语言混、格式乱），R1 靠「少量冷启动 SFT + 多阶段 RL + 蒸馏」把它收成能上线的产品。要讲清冷启动数据的作用是「定格式定语言」而非「教推理」。</p>"
 "<h2>R1-Zero：能力足、可用差</h2>"
 "<p>R1-Zero 是纯 RL 跑出来的，推理能力强，涌现了反思和长思维链。但它的问题很现实：输出经常中英文夹杂、思考过程啰嗦且格式不规整、可读性差，甚至会出现语言切换混乱。对研究它是震撼的证明，对实际使用它不够好用——用户要的是清晰、稳定、规范的可读输出，不是一个逻辑对但读不下去的乱码式推理。这是 R1 要解决的核心问题。</p>"
 "<h2>R1 的收口：冷启动数据 + 多阶段</h2>"
 "<p>R1 的改进分几步。第一步是冷启动数据：用少量高质量的、带推理过程的样本做一轮 SFT，给模型一个「该怎么规范输出」的起点，避免从纯 RL 那个杂乱状态起步。第二步是在这个起点上再做大规模 RL，让推理能力继续长，同时用格式奖励等把输出规范住。第三步是把练好的推理能力蒸馏到更小的模型（基于不同体量的开源底座），让小模型也能有不错的推理表现，覆盖更多部署场景。</p>"
 "<h2>冷启动数据的作用：定格式，不教推理</h2>"
 "<p>这里有个关键认知：冷启动数据不是「教推理」，推理能力主要还是 RL 涌现的；冷启动数据的作用是「定格式、定语言、定可读性」，把已经涌现的能力包装成可用形态。R1 的报告也强调：R1-Zero 的纯 RL 路线才是能力涌现的根，R1 是在其上做可用性收口。如果把冷启动数据说成「推理能力的来源」，就把因果搞反了。正确的说法是：能力来自纯 RL，冷启动数据让能力变得好用。</p>"
 "<h2>多阶段 RL：推理 + 通用</h2>"
 "<p>R1 不是只做一轮 RL。它在推理 RL 之后，还会再做一轮面向通用能力的 RL——把推理能力和对齐人类偏好、安全性、通用任务能力结合起来，让模型既会推理又不偏科。这种「推理 RL + 通用 RL」的多阶段设计，是 R1 比 R1-Zero 更全面的另一面。R1-Zero 只追推理，所以偏科；R1 既强又稳。</p>"
 "<h2>蒸馏：能力外溢到小模型</h2>"
 "<p>练好 R1 后，把它的推理能力蒸馏到不同体量的开源底座（Qwen、Llama 系列），让小模型也能推理。这是 R1 能力外溢的关键一步，下一题细讲。这里只需记住：R1 不仅是成品，还是蒸馏小模型的「老师」，它的价值通过蒸馏放大到一整个模型矩阵。</p>"
 "<h3>面试口述版</h3><p>R1-Zero 和 R1 的关系，我概括成「Zero 证明能力、R1 解决可用」。R1-Zero 纯 RL 跑出来，推理很强，但输出中英夹杂、啰嗦、格式乱，研究上震撼、实用上不行。R1 的收口分几步：先拿少量高质量带推理的样本做一轮冷启动 SFT，作用不是教推理，而是给模型一个规范输出的起点、定格式定语言；在这个起点上再大规模 RL 让能力继续长、同时用格式奖励把输出收住；之后还做一轮通用 RL 把推理和对齐、安全、通用能力结合，让模型既会推理又不偏科——这是 R1 比 Zero 更全面的一面；最后把推理能力蒸馏到小模型覆盖更多部署场景。所以能力主要还是 RL 涌现的，冷启动数据干的是包装成可用形态的活，R1 相对 Zero 是工程可用性的收口。R1 不仅是成品，还是蒸馏小模型的老师，价值通过蒸馏放大到一整个模型矩阵。</p>"),

"ds5q08fp8": ("8.DeepSeek的FP8混合精度训练",
 "<h3>答题思路</h3><p>这道题考的是训练侧的工程硬功夫。回答要讲清楚为什么 FP8 难、DeepSeek 怎么做到既省又稳。核心是「混合」二字——不是无脑全 FP8，而是关键计算保留高精度、对数值不敏感的部分降到 FP8，再配合细粒度的缩放因子。</p>"
 "<h2>为什么 FP8 难：精度与稳定</h2>"
 "<p>FP8 只有 8 bit 表示，动态范围和精度都比 BF16/FP16 小很多。FP8 常见有 E4M3（4 位指数 3 位尾数）和 E5M2（5 位指数 2 位尾数）两种格式：E4M3 精度高但范围小，E5M2 范围大但精度低。直接全量 FP8 训练大模型，激活和梯度的极值容易被截断或下溢，导致训练发散或质量掉。业界长期对 FP8 训练持谨慎态度，多用在做推理、或部分算子上，全量训练大模型是前沿工程。</p>"
 "<h2>混合精度策略：哪里降、哪里留</h2>"
 "<p>DeepSeek-V3 的做法是「混合」：对数值范围可控、对精度不敏感的骨干矩阵乘（比如 MoE 专家里的线性层）降到 FP8，省算力省通信；而对精度敏感的关键环节——比如部分激活、累加、归一化、以及主 logit 的计算——保留高精度（BF16/FP32）。具体说，FP8 主要用在前向和反向的 GEMM（矩阵乘）上，而涉及累加和数值敏感的操作走高精度。这种「敏感留高精度 + 不敏感降 FP8」的分工，让算力收益最大化的同时把数值风险压住。</p>"
 "<h2>细粒度缩放：让 FP8 真能用</h2>"
 "<p>光降到 FP8 还不够，因为一张大张量里的数值范围可能很宽，一个全局缩放因子会让小数被截断、大数被下溢。DeepSeek 配合「细粒度缩放」：不是整张张量用一个缩放因子，而是按更小的块（比如每 128 个元素一块，或按行/按 tile）分别算缩放因子，让每个小区域的数值都落到 FP8 能表示的最佳范围内，减少截断和下溢。这是 FP8 在大模型训练里能稳的关键工程技巧——没有细粒度缩放，FP8 很容易在极端值上炸。</p>"
 "<h2>E4M3 还是 E5M2</h2>"
 "<p>DeepSeek 主要用 E4M3（精度优先，因为前向激活范围相对可控），在需要更大动态范围的地方（如部分梯度）才用 E5M2 或回退 BF16。这种格式选择也是「精度优先、范围兜底」的思路。回答时不用背所有细节，但要能说出 FP8 有两种格式、各有取舍、DeepSeek 按场景选。</p>"
 "<h2>收益与意义</h2>"
 "<p>这套「敏感留高精度 + 不敏感降 FP8 + 细粒度缩放」的组合，让 V3 在大规模训练里把矩阵乘和跨卡通信都压到 8 bit，训练成本显著下降（V3 全量训练约 553 万 H800 小时）。这是它训练成本低的一个重要支撑，也是把 FP8 真正用到生产级大模型训练的代表性实践——它证明了 FP8 训练在工程上可行、能省钱、还能不掉质量，对整个开源社区的工程实践有示范意义。</p>"
 "<h3>面试口述版</h3><p>FP8 训练难在它只有 8 bit、动态范围小，有 E4M3 和 E5M2 两种格式，精度高的范围小、范围大的精度低，全量降下去极值容易截断或下溢，训练会发散或掉点。DeepSeek 的做法核心是「混合」两个字，不是无脑全 FP8。骨干的、对精度不敏感的矩阵乘降到 FP8 省算力省通信；对精度敏感的环节——累加、归一化、主 logit 这些——保留高精度。再加上细粒度缩放：不是整张张量一个缩放因子，而是按小块分别算缩放，让每个小区域数值都落进 FP8 表示范围，这是 FP8 能稳的关键，没有它很容易在极端值上炸。格式上主要用 E4M3 精度优先，需要大范围的地方才用 E5M2 或回退。这套敏感留高精度、不敏感降 FP8、细粒度缩放，让 V3 把矩阵乘和跨卡通信都压到 8 bit，训练成本数量级下降，约 553 万 H800 小时跑完全量，也证明了 FP8 在生产级大模型训练可行、能省钱、不掉质量，对开源社区工程实践有示范意义。</p>"),

"ds5q09mtp": ("9.多Token预测MTP",
 "<h3>答题思路</h3><p>这道题考的是 MTP 在训练和推理两端的作用。回答要讲清两件事：训练上它让信号密度提升、模型学得更准；推理上它支持投机解码、把解码从一次一个 token 变成一次多个，吞吐提升。注意区分 MTP 和「自回归」的关系——它是在自回归基础上加的并行预测头，不是替代自回归。</p>"
 "<h2>训练侧：信号更密、表示更强</h2>"
 "<p>标准语言模型训练时每个位置只预测下一个 token，一个 token 的监督信号就一个。MTP 让每个位置同时预测后续若干个 token（比如后面 2、3 个），等于把一个位置的监督信号翻倍，训练信号更密。更密的好处不仅是效率，还有表示质量——要同时预测多个未来 token，模型得学到更前瞻、更结构化的表示，不能只顾眼前一个 token，这有正则化效果，能缓解「短视」，提升长程一致性。</p>"
 "<h2>推理侧：投机解码加速</h2>"
 "<p>推理时标准自回归是串行一次一个 token，这是解码慢的根本——每生成一个 token 都要跑一遍前向。MTP 训出来的「预测后续多个 token」的头，在推理时可以用来做投机解码：先用 MTP 头一次性「猜」出后续若干 token，再用主模型并行验证（一次前向验证多个候选），对的接受、错的回退重算。一次接受多个 token，解码吞吐显著提升。这对长文本生成（客服、写作、代码）体验提升明显，是 DeepSeek 解码快的一个重要原因。</p>"
 "<h2>MTP 头的结构：额外轻量模块</h2>"
 "<p>一个常被追问的点：MTP 头和主模型的关系。MTP 头是额外的轻量预测模块，和主干共享表示（或接主干隐藏状态），但在每个位置额外产出「后续多 token」的分布；训练时联合优化，推理时可选择用或不用——不用就退回标准自回归，用了就加速。所以它是个「可选增益」设计，不破坏主模型，可平滑开关。</p>"
 "<h2>和 Speculative Decoding 的小模型路线的区别</h2>"
 "<p>投机解码经典做法是「大模型 + 小模型」：小模型猜、大模型验。MTP 的区别是它不需要额外小模型，预测头本身就是主模型的一部分，共享表示，省了一份模型。这是 DeepSeek 把投机解码「内置」到训练里的设计——训练时就把多 token 预测头练好，推理时直接用，不需要再单独训小模型。工程上更简洁。</p>"
 "<h2>深度预测的表示收益</h2>"
 "<p>多 token 预测还逼模型学「更深」的表示：预测下一个 token 可以靠局部 n-gram 统计凑合，但预测后面 2、3 个 token 就必须理解更全局的上下文和结构。所以 MTP 在表示学习上有「逼深」的作用，这是它训练收益的另一面，不只是信号密度。</p>"
 "<h3>面试口述版</h3><p>MTP 我分两端讲。训练端：标准自回归每个位置只预测下一个 token，一个位置一个监督信号；MTP 让每个位置同时预测后面好几个 token，信号变密，而且要预测多个未来 token 就逼模型学更前瞻更结构化的表示，有正则效果、能缓解短视。推理端：自回归解码慢在串行一次一个 token 每个都要跑前向，MTP 训出来的预测头能做投机解码——先用它一次猜出后续几个 token，主模型并行验证，对的接受、错的回退，一次吞多个 token，吞吐上来。MTP 头是额外轻量模块、和主干共享表示，训练联合优化、推理可选开关，不用就退回标准自回归，是个可选增益设计。和经典投机解码的区别是经典要大模型加小模型，MTP 把预测头内置进主模型共享表示、省了一份小模型，工程更简洁。多 token 预测还逼模型学更深的表示——预测下一个能靠局部统计凑合，预测后面两三个必须懂全局结构，这是训练收益的另一面。这也是 DeepSeek 解码快的一个原因。</p>"),

"ds5q10ctx": ("10.DeepSeek长上下文与KV缓存管理",
 "<h3>答题思路</h3><p>这道题把 MLA 和长上下文串起来考。答的核心链路是：长上下文的瓶颈在 KV 缓存 → MLA 把它压到四分之一 → 配合上下文缓存复用 → 支撑超长文档场景。要讲清「为什么长上下文贵」和「MLA+工程怎么解」，以及 RAG 与长上下文的关系。</p>"
 "<h2>瓶颈：长上下文 = KV 线性增长</h2>"
 "<p>注意力模型推理时，每个历史 token 都要在每层存 K、V。上下文越长，KV 缓存越大，和长度成线性关系。长文档、多轮对话、大代码库这类场景，KV 缓存会撑爆显存，也拖慢每一步注意力计算——因为每生成一个新 token，要和所有历史算注意力，长度越长算得越久。这是长上下文又慢又贵又难撑的根，也是「上下文越长、单位 token 成本越高」的原因。</p>"
 "<h2>解法一：MLA 压缩</h2>"
 "<p>DeepSeek 靠两招。第一招是 MLA：KV 压到潜在向量，缓存降到约四分之一，同样显存撑更长上下文、更大 batch。这点前面讲过。MLA 的意义在长上下文场景尤其大——KV 缓存是长上下文的头号开销，压到四分之一等于把长上下文的显存门槛和并发能力一起抬上来。</p>"
 "<h2>解法二：上下文缓存复用</h2>"
 "<p>第二招是工程上的上下文缓存复用：长对话、RAG 这类场景里，前缀（系统提示、文档内容、历史轮）在多次请求间是重复的，这部分的前向计算和 KV 可以缓存复用，不用每次重算。配合 MLA 后单份缓存小，复用收益更大——以前一份前缀缓存占的显存，现在能多缓存几份。这对 RAG 和多轮对话的吞吐提升是实打实的。</p>"
 "<h2>长上下文 vs RAG：不是替代</h2>"
 "<p>一个常被讨论的点：有了超长上下文还要 RAG 吗？答案是互补不是替代。长上下文适合「一次性把材料全塞进去」的场景，简单直接，但成本随长度涨、且每次请求都要重算（除非缓存复用）。RAG 适合「知识库大、只取相关片段」的场景，成本低、知识可无限扩，但要做检索、有召回不准的风险。DeepSeek 的长上下文 + 缓存复用让长上下文方案更划算，但 RAG 在大知识库下仍是更经济的选择。成熟做法是两者结合：RAG 召回相关片段 + 长上下文一次性消化。</p>"
 "<h2>长上下文的评估陷阱</h2>"
 "<p>面试时可以提一句：长上下文的 benchmark 要看「大海捞针」（needle in haystack）这种测在长文档里找特定信息的能力，还要看「有效长度」——很多模型声称 128k 上下文，但有效利用的可能远低于标称。MLA 压缓存是支撑标称长度的基础，但实际有效长度还看训练时的长文本数据和质量。DeepSeek 在长上下文 benchmark 上表现好，是 MLA + 训练数据 + 缓存复用三者合力的结果。</p>"
 "<h3>面试口述版</h3><p>长上下文的瓶颈在 KV 缓存随长度线性增长，长文档、多轮对话这些场景显存容易爆、每步注意力也慢，这是长上下文又慢又贵又难撑的根。DeepSeek 解它靠两招。第一是 MLA，把 KV 压成潜在向量，缓存降到四分之一，同样显存撑更长上下文更大并发，MLA 在长上下文场景收益尤其大，因为 KV 缓存就是头号开销。第二是工程上的缓存复用：长对话和 RAG 里系统提示、文档、历史轮这些前缀在多次请求间重复，把这部分前向和 KV 缓存复用不重算，配 MLA 单份缓存小、能多缓存几份，吞吐实打实提升。长上下文和 RAG 不是替代是互补：长上下文适合一次性塞材料但成本随长度涨，RAG 适合大知识库只取相关片段成本低但要检索、有召回风险，成熟做法是 RAG 召回加长上下文消化。另外长上下文要看大海捞针测和有效长度，很多模型标称 128k 实际有效远低，DeepSeek 表现好是 MLA 加训练数据加缓存复用三者合力。</p>"),

"ds5q11distill": ("11.DeepSeek-R1推理能力的蒸馏",
 "<h3>答题思路</h3><p>这道题考的是 R1 的能力外溢——把大模型的推理能力传给小模型。回答要讲清蒸馏的对象（R1 当老师）、方式（生成思维链数据再 SFT）、以及「蒸馏 vs 直接 RL」的关键区别，还有蒸馏的边界（小模型容量天花板）。</p>"
 "<h2>蒸馏：用 R1 生成带推理的数据训小模型</h2>"
 "<p>DeepSeek-R1 练好后，拿它当老师，对大量问题生成带完整推理过程的回答（思维链数据），再把这些数据用 SFT 的方式训给若干更小的开源底座（不同体量的 Qwen、Llama 系列）。小模型学了这些高质量的推理样本后，推理表现明显提升——比小模型自己直接做 RL 效果更好、成本也低得多。这是把大模型涌现的推理能力「规模化复制」到小模型的高效路径。</p>"
 "<h2>为什么蒸馏比小模型直接 RL 更划算</h2>"
 "<p>直接在小模型上做大规模 RL 难度大、成本高、且小模型容量有限，纯 RL 不一定能涌现出强推理——R1-Zero 能涌现是因为 V3 基座够大，小模型没有这个容量基础。而蒸馏让小模型直接继承 R1 已经涌现的能力，等于走「能力复制」而非「能力重练」的捷径。R1 报告也验证了：蒸馏出的小模型在推理 benchmark 上超过不少同体量模型，甚至超过一些更大的模型。这说明大模型的推理能力可通过数据这个载体高效传递。</p>"
 "<h2>蒸馏的边界：容量天花板</h2>"
 "<p>但蒸馏有边界：小模型容量有限，蒸馏能逼近其容量天花板，但突破不了天花板。R1 报告里有个有意思的发现——蒸馏到的小模型在某些推理 benchmark 上甚至优于用 RL 训的同体量小模型（那些没先蒸馏直接 RL 的）。但无论如何蒸馏，小模型终究比不过 R1 本体。所以蒸馏是「在给定小模型容量下榨干推理能力」的高效手段，不是「把小模型变成 R1」。</p>"
 "<h2>蒸馏数据的质量是关键</h2>"
 "<p>蒸馏效果好坏，很大程度取决于老师生成的思维链数据质量——推理过程要对、要清晰、要多样。R1 当老师的好处是它推理强、生成质量高，而且可以大规模生成覆盖各类问题。数据质量 + 规模 + 多样性，三者齐了，蒸馏才有效。这也是为什么「找一个强老师」比「找一个强学生」更重要。</p>"
 "<h2>定位互补：云端顶配 + 端侧轻量</h2>"
 "<p>一个延伸点：蒸馏后的小模型和 R1 本身的定位互补——R1 顶配强但重，小模型轻、便宜、能本地部署，覆盖端侧和成本敏感场景。这构成了一套从云端顶配到端侧轻量的推理能力产品线，让 R1 的价值不局限在一个大模型，而是扩散到一整个矩阵。</p>"
 "<h3>面试口述版</h3><p>R1 蒸馏的思路是拿 R1 当老师，对大量问题生成带完整推理过程的思维链数据，再 SFT 给更小的开源底座，把已经涌现的推理能力规模化复制过去。为什么不用小模型直接 RL？因为小模型容量有限，R1-Zero 能涌现是 V3 基座够大，小模型没这个容量基础，纯 RL 不一定涌现得出还贵；蒸馏走的是能力复制不是能力重练，又快又省。报告也验证蒸馏出的小模型推理超不少同体量甚至更大的模型，有个有意思的发现是蒸馏的小模型在某些 benchmark 上甚至优于直接 RL 的同体量小模型。但蒸馏有边界：小模型容量天花板突破不了，终究比不过 R1 本体，所以蒸馏是在给定容量下榨干推理能力的高效手段，不是把小模型变成 R1。效果好坏很大程度取决于老师生成的思维链数据质量——推理要对要清晰要多样，R1 当老师就是因为它强能大规模生成。定位上蒸馏小模型和 R1 互补：R1 顶配强但重，小模型轻便宜能本地部署覆盖端侧，构成从云端到端侧的推理能力产品线。</p>"),

"ds5q12cost": ("12.DeepSeek为何又强又便宜",
 "<h3>答题思路</h3><p>这道题是综合题，把前面所有点收口到「成本」这条主线。回答要避免空泛说「工程好」，要逐项落到具体技术省了哪笔账：MoE 省激活算力、MLA 省显存、FP8 省训练算力与通信、MTP 提解码吞吐、无损均衡稳训练不返工、GRPO 省 critic。最后点出这是系统性协同而非单点。</p>"
 "<h2>省算力：MoE + FP8</h2>"
 "<p>推理侧，MoE 让 671B 模型每 token 只激活 37B，单位 token 计算量远低于全量模型，这是「大而快」的根——同样生成一个 token，它实际算的参数量只有稠密 671B 的约 5.5%。训练侧，FP8 混合精度把骨干矩阵乘和跨卡通信压到 8 bit，训练总算力和通信带宽都显著下降。这两项直接省了最大头的算力和带宽。</p>"
 "<h2>省显存：MLA + 缓存复用</h2>"
 "<p>MLA 把 KV 缓存压到约四分之一，长上下文和并发都撑得住，同样硬件跑更多请求。配合前缀缓存复用，重复前缀不重算。显存省下来意味着更大 batch、更高吞吐，单位成本再降。在并发服务场景，KV 缓存是显存大头，压到四分之一等于把能同时服务的请求数翻几倍，这是单位成本下降的关键。</p>"
 "<h2>提密度：MTP + GRPO + 无损均衡</h2>"
 "<p>MTP 让训练信号更密、推理能投机解码，两头都提效率——训练数据利用更充分、推理解码吞吐上来。GRPO 省 critic，RL 训练算力省一半，是大规模 RL 能跑得起的算法前提，没有 GRPO，R1 这种规模的纯 RL 在工程上可能不现实。无损负载均衡让 MoE 训练稳、不因负载不均返工，专家利用率高、不浪费算力——返工是训练成本的大坑，无损均衡从根上避了。这几项叠加，训练不返工、推理高吞吐。</p>"
 "<h2>训练成本的可量化</h2>"
 "<p>把这些点合起来看：DeepSeek 的便宜不是某一处讨巧，而是从架构（MoE/MLA）、训练（FP8/GRPO/无损均衡）、推理（MTP/缓存复用）三层协同省出来的。可量化的结果是：V3 全量训练约 553 万 H800 小时，相比同体量模型省了一个数量级；推理侧激活参数压到 5.5%，加上 MLA 让并发翻倍，单位 token 服务成本远低于同档模型。这种系统性优化叠加，才有了它「同体量里成本最低、效果却压着很多更贵模型」的结果。</p>"
 "<h2>对行业的启示</h2>"
 "<p>DeepSeek 的路径证明了一件事：在大模型赛道，纯堆参数和算力不是唯一出路，架构和工程的系统性创新能把成本打到数量级低，同时保持竞争力。这对开源社区和中小团队是重大利好——成本下来后，更多玩家能参与，应用层创新会被释放。这也是为什么 DeepSeek 发布后对行业冲击这么大：它重新定义了「强模型需要花多少钱」的预期。</p>"
 "<h3>面试口述版</h3><p>DeepSeek 又强又便宜，我会沿着「省了哪笔账」这条线收口。省算力：MoE 让 671B 只激活 37B，单位 token 实际算的参数只有稠密 671B 的约 5.5%；FP8 把训练矩阵乘和跨卡通信压到 8 bit，训练总算力带宽都降。省显存：MLA 把 KV 缓存压到四分之一，长上下文和并发都撑得住，并发服务请求数能翻几倍，同样硬件跑更多请求；再配前缀缓存复用，重复部分不重算。提密度：MTP 训练信号密、推理能投机解码；GRPO 省 critic，RL 算力省一半，是大规模 RL 跑得起的前提，没有它 R1 这种规模纯 RL 工程上可能不现实；无损负载均衡让 MoE 训练稳不返工、专家利用率高，返工是训练成本大坑、无损均衡从根上避。这些不是单点讨巧，是架构、训练、推理三层协同省出来的系统性结果。可量化：V3 全量训练约 553 万 H800 小时、省一个数量级，推理激活压到 5.5%、并发翻倍，单位成本远低于同档。它证明了大模型赛道不只有堆参数堆算力一条路，架构和工程的系统性创新能把成本打到数量级低还保持竞争力，这也是它发布后冲击这么大的原因。</p>"),
}


def main():
    for slug, (title, html) in DEEPSEEK.items():
        path = os.path.join(DOCS, slug + ".json")
        d = json.load(open(path, encoding="utf-8")) if os.path.exists(path) else {}
        d.update({"_kind": "doc", "slug": slug, "title": title, "content": wrap(html),
                  "format": "lake", "word_count": wc(html), "status": 1})
        json.dump(d, open(path, "w", encoding="utf-8"), ensure_ascii=False)
    print("专题五扩展完成:", len(DEEPSEEK), "篇")


if __name__ == "__main__":
    main()
